import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  basePath: "/digital-experience-courses",
  assetPrefix: "/digital-experience-courses/",
};

export default nextConfig;
